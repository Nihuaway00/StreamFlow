services:
  postgres:
    image: postgres:15-alpine
    container_name: db
    ports:
      - "9999:5432"
    environment:
      POSTGRES_USER: prod
      POSTGRES_PASSWORD: prod
      POSTGRES_DB: streaming_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - streamflow

  nginx:
    container_name: nginx
    build: ./nginx
    environment:
      - BACKEND_HOST=backend
      - BACKEND_PORT=8000
      - FRONTEND_HOST=frontend
      - FRONTEND_PORT=5173
      - STORAGE_HOST=storage
      - STORAGE_PORT=9000
    ports:
      - "80:80"
    networks:
      - streamflow

  backend:
    container_name: backend
    build: ./backend
    environment:
      - DATABASE_URL=postgresql+asyncpg://prod:prod@postgres:5432/streaming_db
      - HLS_BASE_URL=http://$SERVER_HOST:8080
      - RTMP_SERVER_HOST=media-server
      - RTMP_PORT=1935
      - MINIO_ENDPOINT=storage:9000
      - MINIO_ACCESSKEY=test
      - MINIO_SECRETKEY=secure123$
      - MINIO_BUCKET=prod
      - API_HOST=$SERVER_HOST
      - API_PORT=$API_PORT
    depends_on:
      - nginx
      - postgres
      - storage
    networks:
      - streamflow

  media-server:
    container_name: media-server
    build: ./media_server
    environment:
      - BACKEND_HOST=backend
      - BACKEND_PORT=8000
    ports:
      - "1935:1935"
      - "8080:8080"
    volumes:
      - hls_data:/tmp/hls
    networks:
      - streamflow

  frontend:
    container_name: frondend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      VITE_API_URL: http://backend:8000
      VITE_HLS_BASE_URL: http://media-server:8080
    networks:
      - streamflow

  storage:
    image: minio/minio
    container_name: storage
    command: server /data --console-address ":9090"
    restart: always
    volumes:
      - minio_data:/data
    environment:
      - MINIO_ROOT_USER=test
      - MINIO_ROOT_PASSWORD=secure123$
    networks:
      - streamflow

volumes:
  postgres_data:
  hls_data:
  minio_data:

networks:
  streamflow: